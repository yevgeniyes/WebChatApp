WebChatApp.ConsoleClient

This is console analogue of web page client.

Launch WebChatApp.ConsoleClient.exe to start client app. 
Or use web page client by opening "http://localhost:8080/" url in the browser.

To login register new name via Registration or use default names: zero, xman, alpha, smoke.