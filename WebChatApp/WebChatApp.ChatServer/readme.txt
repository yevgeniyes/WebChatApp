WebChatApp.ChatServer

This server receives already valid requests (login, put message, request new messages, logout) and sends response.
Generates user token and stores it during session until logout.

Administrator privileges not required to launch.

Server node stored in WebChatApp.ChatServer.laconf config file.